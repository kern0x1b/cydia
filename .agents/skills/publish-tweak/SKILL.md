---
name: publish-tweak
description: Publish a new or updated tweak .deb to this Cydia repo — copy it into debs/, rebuild the package index and Release, update the README table and depiction, and commit. Use whenever a .deb needs to go live on this source.
---

# Publish a tweak to this Cydia repo

1. Build the `.deb` in the tweak's own source repo first (see that repo's
   README). This repo never builds tweak source — it only distributes
   already-built `.deb`s.

2. From this repo's root, copy the deb in and reindex in one step:
   ```
   ./publish.sh <tweak-repo>/dist/*.deb
   ```
   Run `./publish.sh` with no arguments instead if the `.deb` is already in
   `debs/` and you only need to reindex.

   This copies the file into `debs/`, regenerates `Packages`, `Packages.gz`,
   `Packages.bz2`, and rewrites the checksum block in `Release`. It prints
   every package/version it indexed — check that output names what you
   expect before committing.

3. If this is a new package (not just a version bump), add a page under
   `depictions/<packageid-without-prefix>.html` (copy an existing one as a
   template — it links `../icons/<name>.png` and `style.css`) and drop the
   icon into `icons/`.

4. Add or update the row for the package in the README's "Published tweaks"
   table.

5. Commit everything together and push:
   ```
   git add -A
   git commit -m "Publish <Tweak Name> <version>"
   git push
   ```
   Match the existing commit style: a plain imperative subject, no type
   prefix, naming the tweak and version for a publish commit.

Never hand-edit `Packages` or the checksum block in `Release` — both are
fully regenerated by `publish.sh` and any manual edit is overwritten on the
next run.
